/**
 * View types config
 */

const list = {
  label: 'List',
  key: 'list'
}
const grid = {
  label: 'Grid',
  key: 'grid'
}
const types = [
  list, grid
]

export default types
export { list, grid }
